import Layout from "../components/Layout";
import MoviesList from "../components/MoviesList";

const Home = () => {
    return (
        <Layout>
            <MoviesList />
        </Layout>
    );
}

export default Home;